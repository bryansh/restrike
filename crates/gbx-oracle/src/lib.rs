//! Trace format, comparators, and replay driver for differential testing:
//! checking engine behavior against traces captured from the original game.
//!
//! This crate is platform-pure: no windowing, audio, or async runtime dependencies.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert_eq!(2 + 2, 4);
    }
}
