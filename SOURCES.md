# SOURCES.md — Provenance Ledger

Purpose: track which reference informed each part of this engine — a coab
file, a forum thread, a blog post, a manual page, or our own reverse
engineering — so behavior in this codebase is always traceable to evidence.
Per PLAN.md D11, reference implementations (notably coab) are read for
behavior and cited here, never copied; any logic ported from a
GPL-compatible source (e.g. ssi-engine) gets its own row noting the license
and exact provenance.

Add a row whenever a subsystem, table, or algorithm is implemented from an
external reference. Keep entries small and specific — link to the exact
file/section/thread/post, not just a project name.

## Ledger

| Subsystem | Source | Type | License / terms | Notes |
|---|---|---|---|---|
| `gbx-vm` ECL execution model: fetch/dispatch loop, vector-table block header, operand modes, persistent string registers, compare/IF/skip semantics, nested runs (camp), block-chaining (NEWECL/PROGRAM), text pagination, CotAB opcode table (`0x00`–`0x40`) | coab `engine/ovr003.cs` (`RunEclVm`/`sub_29607`, `SetupCommandTable`, `CmdItem.Skip`, `TryEncamp`, `CMD_*` handlers), `engine/ovr008.cs` (`vm_LoadCmdSets`, `vm_init_ecl`, `load_ecl_dax`), `engine/seg041.cs` (`press_any_key` pagination), `Classes/Opperation.cs`, `Classes/EclBlock.cs` | reference | unclear (transliterated from the disassembled binary) | Read for behavior, no code copied (D11). Findings recorded in [docs/design/vm-scriptmemory.md](docs/design/vm-scriptmemory.md), incl. corrections from the 2026-07-11 adversarial review. |
| ScriptMemory address windows (`0x4B00`/`0x7A00`/`0x7C00`/`0x8000` ranges), write-side-effect cells, named globals | coab `engine/ovr008.cs` (`vm_GetMemoryValueType`/`sub_30723`, `vm_GetMemoryValue`/`sub_30F16`, `vm_SetMemoryValue`, `alter_character`), `Classes/Gbl.cs` field naming | reference | unclear (as above) | Seed for the per-game address map; unknown cells discovered via access log at runtime. |
| M1 step-0 opcode channel classification (65-opcode table, EngineServices candidate surface): character/party records, monster/NPC load, combat entry, treasure/item instantiation, saving throws, map/file loads, save/menu/camp flow, RNG-backed rolls, **and the full CALL (0x2D) hidden-dispatch-table enumeration + the ON GOTO/ON GOSUB out-of-range-selector fall-through confirmation (ride-along, same pass)** | coab `engine/ovr004.cs` (`copy_protection`), `engine/ovr006.cs` (`AfterCombatExpAndTreasure`), `engine/ovr009.cs` (`MainCombatLoop`), `engine/ovr016.cs` (`MakeCamp`), `engine/ovr017.cs` (`load_mob`/`load_npc`, `SaveGame`), `engine/ovr018.cs` (`startGameMenu`, `FreeCurrentPlayer`), `engine/ovr019.cs` (`end_game_text`), `engine/ovr021.cs` (`step_game_time`), `engine/ovr022.cs` (`create_item`), `engine/ovr024.cs` (`roll_dice`, `RollSavingThrow`, `CanHitTarget`), `engine/ovr025.cs` (`PartySummary`, `selectAPlayer`, `reclac_player_values`, `ItemDisplayNameBuild`, `LoadPic`, `display_map_position_time`), `engine/ovr027.cs` (`yes_no`, `sl_select_item`, `displayInput`, `ClearPromptAreaNoUpdate`), `engine/ovr029.cs` (`RedrawView`), `engine/ovr030.cs` (`load_bigpic`, `load_pic_final`, `draw_bigpic`, `DrawMaybeOverlayed`), `engine/ovr031.cs` (`Load3DMap`, `LoadWalldef`, `get_wall_x2`, `getMap_wall_type`), `engine/ovr034.cs` (`chead_cbody_comspr_icon`), `engine/seg042.cs` (`load_decode_dax`), `engine/seg043.cs` (`print_and_exit`, `GetInputKey`, `clear_keyboard`), `engine/seg044.cs` (`PlaySound`, `Sound` enum — CALL case `0x3201`), `engine/seg049.cs` (`SysDelay`/`GameDelay`), `engine/seg051.cs` (`Random`), `Classes/Area2.cs` (`field_800_Get`/`field_67E` mapping), `Classes/MoneySet.cs` (`ClearAll`), `Classes/Player.cs` (`HasAffect`, `SkillLevel`, `thief_skills`, `movement`), `Classes/Item.cs` (`StructSize`, signature only), `Classes/Opperation.cs` (`Word`/`GetCmdValue` — confirms destination operands never dereference; `.Word` requires `highSet`, so an `ImmByte`-moded destination throws in the original) | reference | unclear (as above) | Read for behavior, no code copied (D11). Full per-opcode findings, including two confirmed contradictions of vm-scriptmemory.md v3 (PARLAY/TREASURE presentation-point claims) and 15 new fidelity-docket candidates, recorded in [docs/design/opcode-classification.md](docs/design/opcode-classification.md). CALL (0x2D)'s 7-case dispatch table and the ON GOTO/ON GOSUB out-of-range-selector fall-through (`ovr003.cs:1038-1059`) were added in a later ride-along pass, same document. `engine/seg037.cs`, `engine/seg001.cs`, `engine/ovr020.cs`, `engine/ovr026.cs`, `engine/ovr033.cs` were named by call sites but not independently read. |

| DAX archive container (`gbx-formats`): header/index layout, RLE decompression algorithm, the ECL block 2-byte payload-prefix convention | coab `Classes/DaxFiles/{DaxCache,DaxFileCache,DaxHeaderEntry,DaxArray,EclBlock}.cs`, `engine/seg040.cs` (`LoadDax`), `engine/seg042.cs` (`load_decode_dax`), `engine/ovr008.cs` (`load_ecl_dax:136-154`); cross-checked against ssi-engine (Java, GPL-3) `data/{DAXFile,ContentFile}.java` — an independent transliteration that agrees byte-for-byte on header layout and RLE semantics | reference + cross-check | coab: unclear (as above); ssi-engine: GPL-3.0 (compatible, D2) | Read for behavior, reimplemented independently in `crates/gbx-formats/src/dax.rs` — no code ported from either source (D11). The two-reference agreement is the cross-check the task instructions asked for. |
| ECL block header vector table decoding (`gbx-vm::read_header_vectors`): 5 separately-anchored `vm_LoadCmdSets(1)` calls, not one contiguous 5-operand batch | coab `engine/ovr008.cs` (`vm_init_ecl:89-133`, `vm_LoadCmdSets:9-80`) | reference | unclear (as above) | Read for behavior; the byte-for-byte trace (and the bug it caught in this function's first implementation) is recorded in the function's doc comment and in [docs/census/cotab-v1.3.md](docs/census/cotab-v1.3.md) §9. |
| `gbx-vm::EclMachine` interpreter (`step`/`resume`) and the census top-25 opcode handlers (+ PRINT RETURN/COMPARE AND/LOAD FILES/NEWECL ride-alongs): exact per-opcode operand order, arithmetic/flag semantics (`compare_variables`/`compare_strings` argument-order unwinding), the `GetCmdValue` string-mode-operand throw hazard, `CMD_Call`'s 7-case dispatch, and `CmdItem.Skip`/`SkipNextCommand`'s size-driven (not decode-driven) advance, incl. its unknown-opcode-tolerant skip-target fallback | coab `engine/ovr003.cs` (`CMD_Exit`, `CMD_Goto`, `CMD_Gosub`, `CMD_Compare`, `CMD_AddSubDivMulti`, `CMD_Random`, `CMD_Save`, `CMD_LoadMonster`, `CMD_SetupMonster`, `CMD_Picture`, `CMD_Print`, `CMD_Return`, `CMD_CompareAnd`, `CMD_If`, `CMD_ClearMonsters`, `CMD_NewECL`, `CMD_LoadFiles`, `CMD_Combat`, `CMD_OnGotoGoSub`, `CMD_GetTable`, `CMD_HorizontalMenu`, `CMD_Call`, `CMD_AndOr`, `CMD_PrintReturn`, `CMD_Delay`, `SkipNextCommand`, `CmdItem.Skip`/`.Run`, `RunEclVm`, `SetupCommandTable`), `engine/ovr008.cs` (`vm_LoadCmdSets:9-80`, `vm_GetCmdValue`, `compare_variables:1243-1253`, `compare_strings:1229-1237`), `Classes/Opperation.cs` (full file — `Code`/`Low`/`High`/`Word`/`GetCmdValue()`'s exact `highSet`-gated switch, confirming which operand modes throw vs. resolve) | reference | unclear (as above) | Read for behavior, no code copied (D11). All 65 opcodes were already enumerated in `docs/design/opcode-classification.md`; this pass re-read the specific handler bodies line-by-line to pin exact operand order and arithmetic for the ~31 opcodes this session's `EclMachine` implements, plus `Opperation.cs`'s `GetCmdValue()` switch (confirms `Classes/Opperation.cs:98-130`: mode `0x80` never sets `highSet`, so an unconditional `GetCmdValue()` call on a string-mode operand throws in the original — the `VmError::StringOperandTypeMismatch` docket-item-5 hazard COMPARE AND's conformance test exercises). Findings recorded in `crates/gbx-vm/src/{host,machine,conformance}.rs` doc comments. |

| ECL inline-string decompression (`gbx-formats/src/ecl_text.rs`): the 6-bit-per-character packing scheme for `0x80`-mode string operands, its 63-symbol table, and the confirmation that `0x81` memory strings are plain NUL-terminated ASCII (never compressed) | coab `engine/ovr008.cs` (`DecompressString`, `inflateChar`/`deflateChar`, `LoadCompressedEclString`, `vm_CopyStringFromMemory`/`vm_WriteStringToMemory`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Independently cross-checked against ssi-engine (Java, GPL-3) `src/main/java/engine/script/EclArgument.java` and `src/main/java/shared/GoldboxString.java` (byte-for-byte agreement) and `~/src/goldbox-refs/tools/hackdocs/STRGFORM.TXT` (the sibling "Unlimited Adventures" tool's documentation of the same SSI-wide 6-bit scheme, corroborating the `'@'`-collides-with-terminator quirk). Verified against real CotAB data: 417 real inline strings decompress to 96.2% alphabetic/space content (`gbx-vm/src/real_data_tests.rs`). |
| GEO map block format (`gbx-formats/src/geo.rs`): the `0x402`-byte block layout (2-byte header + four 256-byte planes: N/E wall nibbles, S/W wall nibbles, the `x2` flags byte, door-state 2-bit fields), and the runtime wall/door query semantics (`WallDoorFlagsGet`'s 4-state door model) | coab `Classes/GeoBlock.cs`, `engine/ovr031.cs` (`Load3DMap`, `getMap_wall_type`, `get_wall_x2`, `WallDoorFlagsGet`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Cross-checked against ssi-engine (Java, GPL-3) `src/main/java/data/dungeon/DungeonMap.java` (byte-for-byte plane-offset agreement) and the Jzatopa corpus's Matrix Cubed raw x86-disassembly notes (`SSI-Engine-Full-Play-ability/scripts/geo_inspect.py`, `docs/load3dmap-cluster-resolution-pass-1.md` — real retail bytes from a different Gold Box title, same engine generation). Explicitly **not** derived from `~/src/goldbox-refs/tools/hackdocs/GEO*.TXT`, which was found to describe an unrelated format ("Unlimited Adventures," a separate SSI toolkit) — see `docs/fidelity-docket.md` and the module's own doc comment for the contradiction. Confirmed on real data: every block in `GEO2.DAX` is exactly `0x402` bytes; block 1's wall topology matches two independent features of *Cluebook.pdf*'s printed Tilverton City map (`restrike map`, verified this session — see task summary). |
| `gbx-vm::EclMachine`'s LOAD PIECES (0x37) handler (shares `CMD_LoadFiles` with LOAD FILES/0x21, under-traced by the original M1 step-0 classification pass) | coab `engine/ovr003.cs` (`CMD_LoadFiles`, full body incl. the `0x37`/`gbl.command`-keyed branch: `LoadWalldef` calls and `setBlocks[].Reset()`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Added when `restrike run-script`'s first real-data demo block (M1 task 3) reached this opcode; a new `EngineServices::reset_wall_set` method was added for `setBlocks[].Reset()`, previously undocumented in `opcode-classification.md`. The `area_ptr.field_1CE`/`field_1D0` gate (unmodeled engine state, no `ScriptMemory` address) is treated as always-false, a documented simplification matching LOAD FILES' own precedent — noted in `machine.rs`'s doc comment. |
| M2 renderer & UI-shell design ([docs/design/renderer-ui-shell.md](docs/design/renderer-ui-shell.md)): display/palette model, screen geometry and frame layouts, 8×8 symbol-set routing and boot assets, text system (font, wrap, pacing, pagination), prompt-line input widgets, the walk loop / world menu / door interaction / chain runner, 3D corridor composition (depth slots, ten draw-cell classes, walldef selection), area map, pictures/portraits/sprites/animations, party panel and status line | coab `Classes/Display.cs`, `Classes/DaxFiles/DaxBlock.cs`, `Classes/GeoBlock.cs` (`WallDefs`/`WallDefBlock`/`MapInfo`), `engine/ovr029.cs` (`RedrawView`), `engine/ovr031.cs` (full file: `Draw3dWorld*`, `draw_3D_8x8_titles`, `DrawAreaMap`, `LoadWalldef`, `Load3DMap`, wall queries), `engine/ovr030.cs` (`load_pic_final`, `load_bigpic`, `head_body`, `Show3DSprite`, `DrawMaybeOverlayed`), `engine/ovr038.cs` (`Put8x8Symbol`, `Load8x8D`), `engine/seg037.cs` (frame layouts), `engine/seg040.cs` (blit primitives, clips, `DrawColorBlock`), `engine/seg041.cs` (`Load8x8Tiles`, `display_char01`, `press_any_key`, `displayStringSlow`, `getUserInputString`/`Short`, `DisplayAndPause`, `GameDelay`), `engine/ovr025.cs` (`PartySummary`, `LoadPic`, `display_map_position_time`, `selectAPlayer`), `engine/ovr027.cs` (`displayInput`, `sl_select_item`, `yes_no`, `BuildInputKeys`), `engine/ovr015.cs` (`main_3d_world_menu`, `locked_door`, `MovePartyForward`, `TryStepForward`, `bash_door`, `pick_lock`), `engine/ovr003.cs` (`sub_29758` walk loop, `sub_29677` chain runner, `TryEncamp`, `CMD_Print`), `engine/ovr008.cs` (`sub_30580` encounter-visual dispatch, `sub_304B4` approach distance), `engine/seg001.cs` (boot asset loads) | reference | unclear (as above) | Read for behavior, no code copied (D11). Cross-checked against ssi-engine (Java, GPL-3) `src/main/java/ui/classic/DungeonRenderer.java` (viewport geometry and backdrop structure agree; its flat-color backdrop band colors differ — flagged in the design doc §1.11, not absorbed) and Gold Box Explorer `Common/Plugins/Dax/DaxWallDefFile.cs` (independent agreement on the 156-byte walldef view-class layout, the `blockId*10+n` 8×8-block convention, and the universal-tile block 203). `~/src/goldbox-refs/tools/hackdocs/DRAW18.TXT`/`DRAW23.TXT`/`GEODATA.TXT` were checked and confirmed TLB/FRUA-generation only (not applicable to CotAB's DAX-era formats; relevant at M9). |
| M2 step 1 — 4bpp image block decoder (`gbx-formats/src/image.rs`): the shared `{height, width, x_pos, y_pos, item_count, field_9}` header and packed-pixel layout behind `8X8D*`/`BIGPIC*`/`HEAD*`/`BODY*`/`SKY`, and the masked-color-to-transparency-16 rule | coab `Classes/DaxFiles/DaxBlock.cs` (`DaxBlock(byte[], int, int)`, `DaxToPicture`, `SetMaskedColor`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Cross-checked against Gold Box Explorer `Common/Plugins/Dax/DaxImagePlugin.cs`/`DaxWallDefFile.cs` (independent agreement on the "one 8x8 image per item" framing). Verified on real data: every 8X8D/BIGPIC/HEAD/BODY/SKY block across the CotAB data set decodes without error at sane dimensions (`image.rs`'s local-only test); `8X8D1.DAX` block 201 is explicitly excluded as the mono font's different layout (see the `font.rs` row below). Eyeball-checked via `restrike dump-image`: `HEAD2.DAX` block 0 renders as a recognizable character portrait (dark-haired face against a black background). |
| M2 step 1 — Animated picture decoder (`gbx-formats/src/anim.rs`): the `PIC*`/`SPRIT*`/`FINAL*` per-frame container and the XOR-delta scheme for `PIC`/`FINAL` frames ≥ 1 | coab `engine/ovr030.cs` (`load_pic_final`) | reference | unclear (as above) | Read for behavior, no code copied (D11). The XOR-delta scope (bytes `0..(bpp/2-1)` only, final byte verbatim, `ovr030.cs:107,119-134`) is exercised by a dedicated synthetic fixture per D-UI7. Verified on real data: every PIC/SPRIT block decodes without error, and PIC/FINAL frames share identical dimensions across every frame in every block (the XOR delta's implicit "frame dimensions are effectively required equal" assumption, §1.8) — no FINAL*.DAX files exist in this data set. |
| M2 step 1 — Mono font decoder (`gbx-formats/src/font.rs`): `8X8D1.DAX` block 201's flat 177-glyph × 8-byte table | coab `engine/seg041.cs` (`Load8x8Tiles`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Matches the original's infallible zero-pad-short/ignore-extra behavior exactly rather than erroring (`Load8x8Tiles`'s independent `i < block_size`/`j < 177` loop guards). Verified on real data: the real block is exactly the full `177 * 8 = 1416` bytes (no zero-padding needed). |
| M2 step 1 — Walldef decoder (`gbx-formats/src/walldef.rs`): the `780 * n`-byte, 5-style × 156-tile-id layout | coab `Classes/GeoBlock.cs` (`WallDefs`/`WallDefBlock`) | reference | unclear (as above) | Read for behavior, no code copied (D11). Cross-checked against Gold Box Explorer `Common/Plugins/Dax/DaxWallDefFile.cs` (`loadWallDefs`'s identical 156-byte style-slice iteration). Verified on real data and answered design-doc docket item 11: WALLDEF block id 0 never appears in any of CotAB's six `WALLDEF{2..6}.DAX` files (observed ids: 1-4, 8-14, 16-17), so the GBE-vs-coab block-0 pairing contradiction (§1.11 item 8) is moot for this data set; non-zero multi-wallset blocks do exist (block 14/`WALLDEF5.DAX`, block 17/`WALLDEF6.DAX`, both 2 wallsets). Finding also recorded in `docs/design/renderer-ui-shell.md` §4 item 11. |
| M2 step 1 — `GameData` (`gbx-formats/src/game_data.rs`): the in-memory `(file, block)`-keyed archive set over `DaxArchive` plus the in-memory detection fingerprint | this doc, `docs/design/renderer-ui-shell.md` D-UI1 | original-re | N/A | `GameData` itself performs zero filesystem I/O (D-UI1's "the core does zero I/O"); the one fs-touching helper, `load_dir`, mirrors `detect::detect_dir`'s existing precedent for this crate. `detect::detect_bytes`/`hash_bytes` were added alongside as the pure (non-fs) counterparts `detect_dir`/`hash_file` already had a fs-only version of. |
| M2 step 1 — EGA palette (`gbx-rules/src/palette.rs`): the canonical 16-entry EGA RGB table | coab `Classes/Display.cs:25-26` (`OrigEgaColors`/`egaColors`) | data | uncopyrightable (standard IBM EGA RGB values) | Read for behavior, no code copied (D11); first real `gbx-rules` entry per `docs/design/renderer-ui-shell.md` D-UI5. Cross-checked against the design doc's own citation (§1.1: "color 10 = `{82,255,82}`"). |
| M2 step 2 — `gbx-engine` framebuffer, draw primitives, resident symbol sets, screen frames, and the text system (`framebuffer.rs`/`draw.rs`/`symbols.rs`/`frames.rs`/`text.rs`/`boot.rs`) | coab `Classes/Display.cs` (`SetPixel3`, `DisplayMono8x8`, `SetEgaPalette`, `UpdateStop`/`UpdateStart`), `engine/seg037.cs` (`DrawFrame_Outer`, `draw8x8_03`, the border/frame id tables), `engine/seg040.cs` (`draw_clipped_picture`, `draw_clipped_nodraw`/`draw_clipped_recolor`, `DrawColorBlock`), `engine/ovr038.cs` (`Put8x8Symbol`, `Load8x8D` — the 5-set id routing, `symbol_set_fix`, the hard error on id 0/`0x128..0x7FFF`), `engine/seg041.cs` (`DrawRectangle`, `Load8x8Tiles`, `display_char01`, `displayString`, `displayStringSlow`, `text_skip_space`, `press_any_key`, the three text-region `bounds`), `engine/seg001.cs:305-321` (the boot load sequence: font, set 4, set 0, `SKY` 250/251/252), `engine/ovr030.cs`/`Classes/DaxFiles/DaxBlock.cs` (`Recolor`, the fade/transparent recolor tables and the fade path's 1-in-4 random dither) | reference | unclear (as above) | Read for behavior, no code copied (D11). **Flagged transcription note** (not silently absorbed): `press_any_key`'s literal decompiled overflow check is `if (X > xEnd) { if (X == xEnd && ...) {trim} }` (`seg041.cs:191-198`) — since both branches test the same fixed `X`, the inner `== xEnd` is unreachable as literally read (the outer `>` already excludes equality), making the design doc's named "exact-fit-drop-one-trailing-space case" dead code under a literal transcription. This implementation treats the outer bound as inclusive (`>=`) so the case is reachable and testable, per the design doc's explicit call-out of it as real behavior — most plausibly a decompiler artifact around the original's actual comparison. Docketed in `docs/design/renderer-ui-shell.md` §4 alongside its other open questions, pending a DOSBox confirmation. All other wrap/pagination/pacing semantics (punctuation tokenization, post-wrap space skip, pagination cursor reset + region clear, out-of-region cursor snap, the fractional pacing accumulator) transcribed as read. Hash-golden and unit-test fixtures are entirely synthetic (D10); the local-only boot/demo tests verify against real CotAB data (`~/goldbox-data/cotab`) without committing any of it. |
| M2 step 3 — `gbx-engine` input queue, the five prompt-line widgets, the UI shell state machine + stub-VM walk-loop flows, and movement/door interaction (`input.rs`/`widgets.rs`/`shell.rs`/`vm_stub.rs`/`movement.rs`/`engine.rs`) | coab `engine/ovr027.cs` (`displayInput:132-341`, `BuildInputKeys:59-86`, `sl_select_item:532-673`, `yes_no:676-689`), `engine/seg043.cs` (`GetInputKey:26-65`, `clear_keyboard:88-94`), `engine/seg041.cs` (`getUserInputString`/`getUserInputShort:234-294`, `DisplayAndPause:297-303`), `engine/ovr015.cs` (`main_3d_world_menu:348-465`, `TryStepForward:278-315`, `MovePartyForward:318-345`, `locked_door:468-593`, `bash_door:49-224`, `pick_lock:227-254`), `engine/ovr031.cs` (`WallDoorFlagsGet:181-219`), `engine/ovr003.cs` (`sub_29758` walk loop `:2230-2396`, `sub_29677` chain runner `:2180-2227`, `CMD_NewECL:480-498`, `TryEncamp:1913-1926`, DAMAGE/`party_killed:1682-1704`), `engine/ovr008.cs` (`vm_init_ecl:89-133`, `sub_317AA:1170-1214`, `buildMenuStrings:1129-1165`), `engine/seg001.cs:240-269` (boot spawn `mapPosX=7,mapPosY=13,mapDirection=0`, `can_bash_door`/`can_pick_door`/`can_knock_door` init) | reference | unclear (as above) | Read for behavior, no code copied (D11). A dedicated research pass this session pinned exact line numbers and several details the design doc's §1.5/§1.6 prose only summarized, recorded inline in `widgets.rs`/`movement.rs`/`shell.rs` doc comments; contradictions vs. the design doc are flagged, not silently absorbed, at each point of use — notably: `displayInput`'s Enter-is-inert-when-no-colors-set gate and digit-always-wins-over-word-selection are simplified/omitted here (colors aren't modeled this session; digit precedence *is* implemented); `getUserInputString`'s Esc/Enter are indistinguishable in the original (this implementation keeps them distinct, per the design doc's own explicit naming of Esc as a real outcome — D-UI2 §1.5); `bash_door`'s STR-16/out-of-table asymmetry (reinforced disables `can_bash_door`, normal doesn't) is transcribed exactly. **Fable review finding addressed**: the design doc's "every blocking site is a Widget in `VmPhase::Gate` or `WorldMenu`" claim is refined — `StepFlow`'s locked-door menu parks a Widget directly in its own stage (`StepStage::DoorInteraction`), no VM run involved; `shell.rs`'s module doc comment records the fix (`VmPhase::Gate` generalizes to any flow stage, per the design doc's own suggested alternative). The M2 session's hardcoded resident map (`engine.rs`'s `DEFAULT_GEO_FILE`/`DEFAULT_GEO_BLOCK`) and the step-3 local-only demo's door location (`(7,12)` North edge) were found this session by BFS over real `GEO2.DAX` block 1 using the engine's own `wall_door_flags` query (`demo.rs`'s walk-demo doc comment). |
| M2 step 3 — `gbx-rules` door-bash STR-to-success table (`bash_door.rs`) | coab `engine/ovr015.cs` `bash_door` (`:49-224`, the two STR tables keyed on door strength) and `pick_lock` (`:227-254`); `engine/ovr024.cs` `roll_dice` (`:586`) for the `1dN <= max` roll shape | reference | unclear (as above) | Read for behavior, no code copied (D11); the first `gbx-rules` mechanics table (§1.6/D-UI5's "bash matrix as evidence-tagged data"). Exercised through `gbx-engine`'s M3 party-predicate seam (`movement.rs`'s `PartyPredicates` trait + `DefaultPartyPredicates`) rather than a real roster, which doesn't exist until M3. |
| M2 step 4 — real `EclMachine` binding: `gbx-engine`'s `ScriptMemory`/`EngineServices` implementation (`vmhost.rs`), replacing step 3's `StubVm` in `shell.rs`'s walk-loop flows | coab `engine/ovr008.cs` (`vm_GetMemoryValueType`/`sub_30723`, `vm_GetMemoryValue`/`sub_30F16`, `vm_SetMemoryValue`, `load_ecl_dax:136-154`), `engine/seg001.cs` (`game_area` boot init, `InitFirst`/`InitAgain:276-277,369-370`), `engine/ovr008.cs` `sub_30580` (`:220-276`, encounter-visual dispatch) | reference | unclear (as above) | Read for behavior, no code copied (D11). A dedicated research pass pinned the Area (`0x4B00-0x4EFF`)/Table (`0x7A00-0x7BFF`)/Party (`0x7C00-0x7FFF`)/Global window dispatch and the named Global cells (position, facing, wall queries, the ECL clock cluster, `in_dungeon`, the sound-variant selector); open items (`0x2CB`'s inertness, `game_area`'s literal `1` boot default vs. this session's fixed `2`, `approach_distance`'s exact formula, INPUT NUMBER/STRING's still-unimplemented opcodes) recorded in [docs/design/vm-scriptmemory.md](docs/design/vm-scriptmemory.md) §5 item 8. The M2 halt policy (any `VmError` mid-vector becomes a logged, counted `HaltRecord`, never a hard failure) is D-VM5's own design choice, now exercised against real content: running real `ECL2.DAX` block 1 headlessly hit a genuine, reproducible halt (opcode `0x06` DIVIDE, unimplemented) inside vector 1 (`SearchLocationAddr`) every time it fires. The step-3 local-only walk-and-bash demo (`demo.rs`) was re-run against the real `EclMachine` and now surfaces a citation correction: the real boot vector's own `0xC04D` write decodes to facing **East**, not the North read from `seg001.cs:250-252` in step 3 — see [docs/design/renderer-ui-shell.md](docs/design/renderer-ui-shell.md) §4 item 14. |
| M2 step 5 — the 3D dungeon-corridor renderer + area map (`gbx-engine`'s `corridor.rs`), and real wallset loading (`vmhost.rs`'s `load_walldef`, `symbols.rs`'s `WallsetSlot`) | coab `engine/ovr031.cs` (`Draw3dWorld:321-370`, `Draw3dWorldBackground:93-137`, `Draw3dWorldFar/Mid/Near:373-640`, `draw_3D_8x8_titles:145-171`, `DrawAreaMap:29-90`, `LoadWalldef:642-687`, `MapCoordIsValid`/`getMap_XXX`/`get_wall_x2:175-318`), `engine/ovr029.cs` (`RedrawView:10-49`, `sky_colours:7-8`), `Classes/GeoBlock.cs` (`WallDefs`/`WallDefBlock.Offset:78-90`), `Classes/Gbl.cs` (`symbol_set_fix:425`, `SetBlock:358-367`), `Classes/Sys.cs` (`WrapMinMax:117-128`), `engine/ovr025.cs` (`LoadPic:1398-1456`, the DungeonMap arm) | reference | unclear (as above) | Read for behavior, no code copied (D11). Two dedicated research passes this session read the full renderer (`ovr031.cs`, every function named above, verbatim for the background/far-front-sweeps/J-filler/area-map/`LoadWalldef`, summarized for the far-side-sweeps — flagged, not silently absorbed, [docs/design/renderer-ui-shell.md](docs/design/renderer-ui-shell.md) §4 item 15) and `RedrawView`/`LoadPic`'s redraw-flag consolidation. Real findings beyond the step-1/M2-design-pass prose: `LoadWalldef` can populate multiple consecutive wallset slots from one call (not just its own `set` argument); the redraw-flag consolidation gate (`CMD_Call` case `0xAE11`) is fully enumerated (`positionChanged`/`byte_1EE91`/`byte_1EE94`/`spriteChanged`/`can_draw_bigpic`, `vmhost.rs`'s `VmMemoryState::clear_redraw_flags`); the confirmed `MapCoordIsValid` bug (checks `mapX >= 0` twice, never `mapY >= 0`) and the confirmed `Draw3dWorldBackground` `get_wall_x2(mapY, mapY)` bug (Y passed twice) are both replicated exactly, not fixed (D11); the `sky_colours` 16-entry table and the sun/moon hour/facing matrix (narrowed South windows, the North branch's own unconditional-on-hour sibling `if`) are transcribed exactly. A local-only real-data demo (`demo.rs`'s `four_facings_at_spawn`) surfaced a genuine engine-only bug (not a coab-fidelity issue): a stale queued `Enter` key, left over from a gate-clearing fallback loop, gets drained by a freshly-created `WorldMenu` hotbar on a later empty-input tick and silently resolves as `'A'` (`ToggleAreaView`) — docketed alongside the design doc's existing drain-to-last-queue uncertainty (§1.11 item 9), worked around locally in the demo, not fixed engine-wide this session. |

### Column guide

- **Subsystem** — the engine area or file this entry covers (e.g. `gbx-vm`
  opcode dispatch, THAC0 table, DAX container format).
- **Source** — the specific reference: repo + file/commit, forum thread +
  post, blog post URL, manual page, or "original RE" for work done directly
  against the binary/data with no secondary source.
- **Type** — one of: `reference` (read for behavior, not copied), `ported`
  (logic adapted from a compatible-license source), `data` (uncopyrightable
  facts/tables extracted from the binary or docs), `original-re` (derived
  from our own disassembly/black-box testing).
- **License / terms** — the source's license, or "N/A" for docs/forums used
  purely as documentation.
- **Notes** — anything a future contributor needs to judge provenance at a
  glance (e.g. "transliterated logic, not copied — see D11").
